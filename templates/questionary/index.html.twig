{% extends 'base.html.twig' %}

{% block title %}Hello QuestionaryController!{% endblock %}

{% block body %}
    <style>
        .example-wrapper {
            margin: 1em auto;
            max-width: 800px;
            width: 95%;
            font: 18px/1.5 sans-serif;
        }

        .example-wrapper code {
            background: #F5F5F5;
            padding: 2px 6px;
        }
    </style>

    <div class="example-wrapper">
        <h1>Hello This is questionary: ✅</h1>

        {% for message in app.flashes('error') %}
            <div style="color: red">
                {{ message }}
            </div>
        {% endfor %}

        <form action="{{ path('create_questionary') }}" method="POST">
            <input type="text" name="fullName">
            <input type="submit">
        </form>
    </div>
{% endblock %}
